export interface agencia {
    nombre: string;
    abreviatura: string;
    direccion: string;
    telefono: string;
}
export interface agenciaDTO {
    uuid: string,
    nombre: string,
    abreviatura: string;
    direccion: string;
    telefono: string;
}
