export interface tipoDocumento {
    nombres: string,
    descripcion: string,
    estado: boolean,
}

export interface tipoDocumentoDTO {
    uuid: string,
    descripcion: string,
    estado: boolean,
  }
  