export interface corte {
    valor: string
    orden: string
    descripcion: string
    monedas:string
    tipoCortes:string
  
}
export interface corteDTO {
    uuid: string
    valor: string
    orden: string
    descripcion: string
    monedas:string
    tipoCortes:string
}
